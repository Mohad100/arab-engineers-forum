@page
@model IndexModel
@{
    ViewData["Title"] = "منتدى المهندسين";
}

<!-- Hero Section -->
<div class="hero-section">
    <div class="container-fluid" style="max-width: 1800px;">
        <div class="row align-items-center py-5">
            <div class="col-lg-6">
                <div class="hero-content">
                    <h1 class="hero-title mb-4">
                        <span class="gradient-text">مرحباً بك في</span><br/>
                        منتدى المهندسين
                    </h1>
                    <p class="hero-subtitle mb-4">
                        انضم إلى مجتمع سيهات لمشاركة الأخبار والأحداث والتواصل مع أهالي المنطقة.
                    </p>
                    @if (User.Identity?.IsAuthenticated == true)
                    {
                        <a asp-page="/CreateThread" class="btn btn-gradient btn-lg px-5 py-3">
                            <i class="bi bi-plus-circle me-2"></i>ابدأ نقاشاً
                        </a>
                    }
                    else
                    {
                        <div class="hero-buttons">
                            <a asp-page="/Register" class="btn btn-gradient btn-lg px-5 py-3 me-3">
                                انضم الآن
                            </a>
                            <a asp-page="/Login" class="btn btn-outline-light btn-lg px-5 py-3">
                                تسجيل الدخول
                            </a>
                        </div>
                    }
                </div>
            </div>
            <div class="col-lg-6">
                <div class="hero-stats">
                    <div class="stat-card">
                        <div class="stat-icon">👥</div>
                        <div class="stat-number">@Model.RecentThreads.Select(t => t.AuthorUsername).Distinct().Count()+</div>
                        <div class="stat-label">الأعضاء النشطون</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-icon">💬</div>
                        <div class="stat-number">@Model.RecentThreads.Count</div>
                        <div class="stat-label">المناقشات</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-icon">🔥</div>
                        <div class="stat-number">@Model.RecentThreads.Sum(t => t.ReplyCount)</div>
                        <div class="stat-label">إجمالي الردود</div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="container-fluid my-5" style="max-width: 1800px;">
    @if (!string.IsNullOrEmpty(TempData["SuccessMessage"] as string))
    {
        <div class="alert alert-success alert-dismissible fade show modern-alert" role="alert">
            <i class="bi bi-check-circle-fill me-2"></i>
            @TempData["SuccessMessage"]
            <button type="button" class="btn-close btn-close-white" data-bs-dismiss="alert"></button>
        </div>
    }

    <!-- Quick Stats Dashboard -->
    <div class="row mb-5">
        <div class="col-md-4">
            <div class="card bg-gradient-primary text-white border-0 shadow-sm">
                <div class="card-body text-center p-4">
                    <i class="bi bi-people-fill" style="font-size: 3rem;"></i>
                    <h3 class="mt-3 mb-0">@Model.TotalUsers</h3>
                    <p class="mb-0 opacity-75">إجمالي الأعضاء</p>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card bg-gradient-success text-white border-0 shadow-sm">
                <div class="card-body text-center p-4">
                    <i class="bi bi-chat-dots-fill" style="font-size: 3rem;"></i>
                    <h3 class="mt-3 mb-0">@Model.RecentThreads.Count</h3>
                    <p class="mb-0 opacity-75">المواضيع النشطة</p>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card bg-gradient-info text-white border-0 shadow-sm">
                <div class="card-body text-center p-4">
                    <i class="bi bi-calendar-check-fill" style="font-size: 3rem;"></i>
                    <h3 class="mt-3 mb-0">@Model.ThreadsToday</h3>
                    <p class="mb-0 opacity-75">مواضيع اليوم</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Trending & Hot Topics -->
    <div class="row mb-5">
        <div class="col-lg-6 mb-4">
            <div class="card border-0 shadow-sm h-100">
                <div class="card-header bg-white border-0 pt-4 pb-3">
                    <h4 class="mb-0"><i class="bi bi-fire text-danger me-2"></i>المواضيع الرائجة</h4>
                    <p class="text-muted small mb-0">الأكثر تفاعلاً هذا الأسبوع</p>
                </div>
                <div class="card-body">
                    @if (Model.TrendingThreads.Any())
                    {
                        <div class="list-group list-group-flush">
                            @foreach (var thread in Model.TrendingThreads)
                            {
                                <a asp-page="/Thread" asp-route-id="@thread.Id" class="list-group-item list-group-item-action border-0 px-0">
                                    <div class="d-flex w-100 justify-content-between align-items-start">
                                        <div class="flex-grow-1">
                                            <h6 class="mb-1">@thread.Title</h6>
                                            <small class="text-muted">
                                                <i class="bi bi-person-circle me-1"></i>@thread.AuthorUsername
                                                <i class="bi bi-clock ms-2 me-1"></i>@thread.CreatedAt.ToString("dd/MM/yyyy")
                                            </small>
                                        </div>
                                        <span class="badge bg-primary rounded-pill">@thread.ReplyCount <i class="bi bi-chat-fill ms-1"></i></span>
                                    </div>
                                </a>
                            }
                        </div>
                    }
                    else
                    {
                        <p class="text-muted text-center py-4">لا توجد مواضيع رائجة حالياً</p>
                    }
                </div>
            </div>
        </div>

        <div class="col-lg-6 mb-4">
            <div class="card border-0 shadow-sm h-100">
                <div class="card-header bg-white border-0 pt-4 pb-3">
                    <h4 class="mb-0"><i class="bi bi-lightning-fill text-warning me-2"></i>آخر النشاطات</h4>
                    <p class="text-muted small mb-0">المواضيع الأحدث</p>
                </div>
                <div class="card-body">
                    @if (Model.RecentThreads.Any())
                    {
                        <div class="list-group list-group-flush">
                            @foreach (var thread in Model.RecentThreads)
                            {
                                <a asp-page="/Thread" asp-route-id="@thread.Id" class="list-group-item list-group-item-action border-0 px-0">
                                    <div class="d-flex w-100 justify-content-between align-items-start">
                                        <div class="flex-grow-1">
                                            <h6 class="mb-1">@thread.Title</h6>
                                            <small class="text-muted">
                                                <i class="bi bi-person-circle me-1"></i>@thread.AuthorUsername
                                                <i class="bi bi-clock ms-2 me-1"></i>@thread.CreatedAt.ToString("dd/MM/yyyy HH:mm")
                                            </small>
                                        </div>
                                        <span class="badge bg-secondary rounded-pill">@thread.ReplyCount <i class="bi bi-chat-fill ms-1"></i></span>
                                    </div>
                                </a>
                            }
                        </div>
                    }
                    else
                    {
                        <p class="text-muted text-center py-4">لا توجد مواضيع حديثة</p>
                    }
                </div>
            </div>
        </div>
    </div>

    <!-- Top Contributors -->
    <div class="row mb-5">
        <div class="col-12">
            <div class="card border-0 shadow-sm">
                <div class="card-header bg-white border-0 pt-4 pb-3">
                    <h4 class="mb-0"><i class="bi bi-trophy-fill text-success me-2"></i>أنشط الأعضاء</h4>
                    <p class="text-muted small mb-0">المساهمون الأكثر نشاطاً</p>
                </div>
                <div class="card-body">
                    @if (Model.TopContributors.Any())
                    {
                        <div class="row g-3">
                            @for (int i = 0; i < Model.TopContributors.Count; i++)
                            {
                                var user = Model.TopContributors[i];
                                var medals = new[] { "🥇", "🥈", "🥉", "🏅", "🏅" };
                                <div class="col-md-2">
                                    <div class="text-center p-3 bg-light rounded">
                                        <div class="fs-1 mb-2">@medals[i]</div>
                                        <h6 class="mb-1">@user.Username</h6>
                                        @if (user.IsAdmin)
                                        {
                                            <span class="badge bg-danger small">مدير</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-secondary small">عضو</span>
                                        }
                                    </div>
                                </div>
                            }
                        </div>
                    }
                    else
                    {
                        <p class="text-muted text-center py-4">لا يوجد أعضاء نشطون بعد</p>
                    }
                </div>
            </div>
        </div>
    </div>

    <!-- Categories Section -->
    <div class="section-header mb-5">
        <h2 class="section-title">استكشف الأقسام</h2>
        <p class="section-subtitle">اعثر على المكان المثالي لبدء محادثتك</p>
    </div>

    <div class="category-rows mb-5">
        @foreach (var category in Fourm.Models.ForumCategory.AllCategories)
        {
            var threadCount = Model.CategoryThreadCounts.GetValueOrDefault(category.Id, 0);
            var latestThread = Model.LatestThreadsByCategory.GetValueOrDefault(category.Id);
            var categoryBgClass = $"category-bg-{category.Id}";
            
            <a asp-page="/Forum" asp-route-category="@category.Id" class="category-link">
                <div class="category-row-card @categoryBgClass">
                    <div class="category-row-icon">
                        <span class="category-icon-emoji">@category.Icon</span>
                    </div>
                    <div class="category-row-content">
                        <h5 class="category-row-name">@category.Name</h5>
                        <p class="category-row-desc">@category.Description</p>
                    </div>
                    <div class="category-row-stats">
                        <div class="stat-count">
                            <span class="stat-number">@threadCount</span>
                            <span class="stat-label">موضوع</span>
                        </div>
                    </div>
                    @if (latestThread != null)
                    {
                        <div class="category-row-latest">
                            <div class="latest-thread-info">
                                <div class="latest-title">@latestThread.Title.Substring(0, Math.Min(40, latestThread.Title.Length))@(latestThread.Title.Length > 40 ? "..." : "")</div>
                                <div class="latest-meta">
                                    <span class="latest-author">@@@latestThread.AuthorUsername</span>
                                    <span class="latest-time">@latestThread.CreatedAt.ToString("MMM dd, yyyy")</span>
                                </div>
                            </div>
                        </div>
                    }
                    else
                    {
                        <div class="category-row-latest">
                            <div class="empty-state">
                                <span>لا توجد مشاركات بعد</span>
                            </div>
                        </div>
                    }
                </div>
            </a>
        }
    </div>
</div>
