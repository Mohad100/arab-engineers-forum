@page
@model RegisterPageModel
@{
    ViewData["Title"] = "التسجيل";
}

<div class="row justify-content-center">
    <div class="col-md-6 col-lg-5">
        <div class="card shadow-sm border-0 mt-5">
            <div class="card-body p-5">
                <h2 class="text-center mb-4 fw-bold text-primary">انضم إلينا! ✨</h2>
                <p class="text-center text-muted mb-4">
                    أنشئ حسابك وكن جزءاً من مجتمعنا الرائع!
                </p>

                @if (!string.IsNullOrEmpty(Model.ErrorMessage))
                {
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        <strong>Oops!</strong> @Model.ErrorMessage
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                }

                <form method="post">
                    <div class="mb-3">
                        <label asp-for="Input.Username" class="form-label fw-semibold">اسم المستخدم</label>
                        <input asp-for="Input.Username" class="form-control form-control-lg rounded-pill" placeholder="اختر اسم مستخدم مميز" />
                        <span asp-validation-for="Input.Username" class="text-danger small"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Input.Email" class="form-label fw-semibold">البريد الإلكتروني (اختياري)</label>
                        <input asp-for="Input.Email" class="form-control form-control-lg rounded-pill" placeholder="your@email.com" />
                        <span asp-validation-for="Input.Email" class="text-danger small"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Input.Password" class="form-label fw-semibold">كلمة المرور</label>
                        <input asp-for="Input.Password" class="form-control form-control-lg rounded-pill" placeholder="6 أحرف على الأقل" />
                        <span asp-validation-for="Input.Password" class="text-danger small"></span>
                    </div>

                    <div class="mb-4">
                        <label asp-for="Input.ConfirmPassword" class="form-label fw-semibold">تأكيد كلمة المرور</label>
                        <input asp-for="Input.ConfirmPassword" class="form-control form-control-lg rounded-pill" placeholder="أعد كتابتها للتأكيد" />
                        <span asp-validation-for="Input.ConfirmPassword" class="text-danger small"></span>
                    </div>

                    <div class="mb-4">
                        <div class="form-check">
                            <input asp-for="Input.AcceptTerms" class="form-check-input" type="checkbox" />
                            <label asp-for="Input.AcceptTerms" class="form-check-label">
                                أوافق على <a href="/Terms" target="_blank" class="text-primary fw-semibold">الشروط والأحكام</a> وأتعهد بالالتزام بالقيم الإسلامية والعربية
                            </label>
                        </div>
                        <span asp-validation-for="Input.AcceptTerms" class="text-danger small d-block mt-1"></span>
                    </div>

                    <div class="d-grid">
                        <button type="submit" class="btn btn-primary btn-lg rounded-pill">
                            إنشاء حسابي 🎉
                        </button>
                    </div>
                </form>

                <hr class="my-4" />

                <p class="text-center text-muted mb-0">
                    لديك حساب بالفعل؟ 
                    <a asp-page="/Login" class="text-decoration-none fw-semibold">سجل الدخول هنا</a>
                </p>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
